一、A2A 协议概述
1.1 什么是 A2A（Agent-to-Agent）协议
Google 于 2025 年 4 月发布的开放互操作协议
Agent 间通信的标准化"网络层"
核心理念：不同框架、不同厂商的 Agent 能够发现、通信、协作
类比：HTTP 标准化了浏览器与服务器的通信，A2A 标准化了 Agent 间的通信
1.2 A2A 的发展历程
2025.04 Google Cloud Next '25 发布，50+ 合作伙伴
2025.06 捐赠给 Linux Foundation
2025 年中 合作伙伴超过 150 家（AWS、Cisco、Microsoft、Salesforce、SAP 等）
协议版本演进
1.3 A2A 的核心价值
跨框架互操作（LangGraph Agent ↔ CrewAI Agent ↔ AutoGen Agent）
跨组织协作（企业 A 的 Agent ↔ 企业 B 的 Agent）
不暴露内部状态（Agent 作为不透明服务）
安全的任务委派与结果交换
1.4 A2A 与 MCP 的关系
MCP：Agent 与工具/数据的连接（垂直集成）
A2A：Agent 与 Agent 的连接（水平协作）
两者互补而非竞争
协议栈定位：MCP 在下层（工具层），A2A 在上层（协作层）
1.5 A2A 的设计原则
不透明性（Opaque）：不暴露 Agent 内部实现
安全性（Secure）：企业级认证与授权
异步优先（Async-first）：支持长时间运行的任务
多模态（Multimodal）：支持文本、图像、音频、视频等

二、A2A 架构设计
2.1 角色模型
Client Agent（客户端 Agent）：发起任务请求
Remote Agent（远程 Agent）：接收并执行任务
A2A Server：托管 Remote Agent 的服务端点
2.2 通信协议
基于 HTTP + JSON-RPC 2.0
Server-Sent Events (SSE) 支持流式通信
标准 HTTP 方法（POST）
2.3 连接模型
Client 通过 HTTP 连接到 Remote Agent 的 A2A Server
无需共享内部状态、记忆或工具
有状态的任务会话

三、核心概念
3.1 Agent Card（Agent 名片）
Agent 的能力描述文档
托管在 /.well-known/agent.json
包含信息
(1) name：Agent 名称
(2) description：能力描述
(3) url：A2A 端点地址
(4) skills：技能列表（名称、描述、输入/输出模式）
(5) authentication：认证方式
(6) capabilities：支持的协议特性（streaming / pushNotifications）
Agent 发现机制
3.2 Task（任务）
A2A 协作的核心工作单元
任务生命周期
(1) submitted：已提交
(2) working：执行中
(3) input-required：需要额外输入
(4) completed：已完成
(5) failed：失败
(6) canceled：已取消
任务 ID 与会话管理
任务的创建、查询、取消
3.3 Message（消息）
Agent 间的信息交换单元
消息角色：user（Client Agent）/ agent（Remote Agent）
消息包含一个或多个 Part
3.4 Part（内容部分）
消息的具体内容载体
Part 类型
(1) TextPart：纯文本
(2) FilePart：文件（内联 base64 或 URI 引用）
(3) DataPart：结构化数据（JSON）
支持多模态内容
3.5 Artifact（产物）
任务的最终输出结果
与中间消息的区别（Artifact 是最终交付物）
Artifact 也由 Part 组成
支持多个 Artifact（一个任务可产出多个结果）
3.6 Push Notification（推送通知）
Remote Agent 主动推送任务状态更新
类似 Webhook 机制
Client 注册推送 URL
适用于长时间运行的异步任务

四、A2A 交互流程
4.1 Agent 发现
Client 获取 Remote Agent 的 Agent Card
解析能力描述，判断是否匹配需求
4.2 任务发送
Client 创建 Task，发送给 Remote Agent
包含任务描述（Message + Parts）
4.3 任务执行
Remote Agent 处理任务
可能需要多轮交互（input-required 状态）
支持流式更新（SSE）
4.4 结果返回
Remote Agent 返回 Artifact
任务状态更新为 completed
4.5 完整交互示例
发现 → 发送任务 → 多轮交互 → 获取结果

五、A2A Server 开发
5.1 Server 实现要点
HTTP 端点设置
JSON-RPC 请求处理
Agent Card 托管
任务状态管理
5.2 Python SDK
google-a2a 包
AgentExecutor 实现
任务处理器注册
5.3 TypeScript SDK
@anthropic-ai/a2a 包（或社区实现）
Server 框架集成
5.4 Agent Card 设计
技能描述的清晰度
输入输出 Schema 定义
认证方式配置

六、A2A Client 开发
6.1 Client 实现要点
Agent Card 获取与解析
任务创建与发送
响应处理（同步 / 流式 / 推送）
6.2 多 Agent 编排
Client 作为编排者调用多个 Remote Agent
任务的并行与串行调度
结果聚合
6.3 与现有框架集成
LangGraph + A2A
CrewAI + A2A
Google ADK + A2A

七、安全与认证
7.1 认证机制
OAuth 2.0
API Key
JWT Token
Agent Card 中的认证声明
7.2 授权控制
任务级权限控制
技能级访问控制
数据访问范围限制
7.3 安全最佳实践
传输层加密（HTTPS）
输入验证
速率限制
审计日志

八、高级特性
8.1 流式交互（Streaming）
SSE 流式任务更新
实时进度反馈
流式 Artifact 传输
8.2 多轮协商
input-required 状态触发多轮交互
Client 与 Remote Agent 的协商对话
动态任务调整
8.3 长时间任务
异步任务执行
Push Notification 状态更新
任务超时与取消
8.4 多模态支持
文本 + 图像 + 文件的混合任务
多模态 Artifact 输出

九、A2A 生态系统
9.1 支持 A2A 的框架与平台
Google ADK（原生支持）
LangGraph（社区集成）
CrewAI（社区集成）
Amazon Bedrock AgentCore
Salesforce Agentforce
9.2 A2A 与其他协议的协同
A2A + MCP：Agent 协作 + 工具集成
A2A + AG-UI：Agent 协作 + 用户界面
三协议协同的完整 Agent 架构
9.3 A2A 的未来演进
协议标准化进程
企业级特性（多租户、合规、审计）
与 Web 标准的融合

十、最佳实践与注意事项
Agent Card 的描述质量决定了被发现和使用的效果
任务设计应当自包含（包含所有必要上下文）
安全性是跨组织协作的前提
合理使用异步模式处理长时间任务
监控 Agent 间的通信质量
常见错误
1. Agent Card 描述模糊导致能力误判
2. 任务上下文不足导致 Remote Agent 无法执行
3. 忽视认证导致安全风险
4. 未处理 input-required 状态导致任务卡死
5. 缺乏超时控制导致任务无限等待
