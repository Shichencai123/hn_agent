一、大文本参数传递概述
1.1 问题定义
Agent 工具调用中的大数据量参数（长文档、大数据集、大型 API 响应）
上下文窗口的 Token 限制与大文本的矛盾
工具返回结果过大导致的上下文膨胀（Context Bloat）
1.2 大文本场景分类
1. 大文本输入（用户提交长文档、大型代码库）
2. 大文本工具参数（向工具传递大量数据）
3. 大文本工具返回（工具返回大量结果）
4. Agent 间传递大数据（多 Agent 协作中的数据交换）
1.3 核心挑战
Token 限制：单次 LLM 调用的上下文窗口有限
上下文腐化（Context Rot）：窗口越长，模型质量越下降
成本膨胀：Token 消耗与成本成正比
延迟增加：大上下文导致推理延迟上升
信噪比下降：大量数据中有效信息占比低

二、大文本输入处理策略
2.1 分块处理（Chunking）
语义分块（按段落、章节、语义边界）
固定大小分块 + 重叠窗口
递归分块（RecursiveCharacterTextSplitter）
分块后的顺序处理或并行处理
2.2 摘要压缩（Summarization）
Map-Reduce 摘要（分块摘要 → 合并摘要）
Refine 摘要（逐块迭代精炼）
Map-Rerank 摘要（分块评分 → 选择最佳）
摘要的信息损失评估
2.3 关键信息提取
LLM 驱动的关键信息提取
基于规则的信息过滤
基于查询的相关性过滤
2.4 分层处理
第一层：快速扫描，提取结构和关键点
第二层：针对关键部分深入分析
第三层：按需获取原始细节

三、大文本工具参数传递
3.1 引用传递模式（Reference Pattern）
不传递原始数据，传递数据引用（URI / ID / 路径）
工具通过引用自行获取数据
大幅减少上下文占用
引用类型
(1) 文件路径引用（file:///path/to/data）
(2) URL 引用（https://...）
(3) 数据库 ID 引用（db://table/id）
(4) 对象存储引用（s3://bucket/key）
(5) 临时存储引用（scratch://session/key）
3.2 Scratchpad / 临时存储模式
Agent 将大数据写入临时存储
后续工具调用通过引用访问
会话结束后自动清理
实现方式
(1) 内存 KV 存储
(2) 临时文件系统
(3) Redis 临时存储
(4) 会话级对象存储
3.3 流式传递模式
大数据通过流式方式逐块传递给工具
工具逐块处理，无需一次性加载全部数据
适用于数据处理管道
3.4 分页传递模式
大数据集分页获取
Agent 按需请求下一页
控制单次传递的数据量
3.5 工具侧过滤模式
将过滤条件传递给工具
工具在数据源侧完成过滤
只返回符合条件的子集
减少数据传输量

四、大文本工具返回处理
4.1 返回结果截断
设置最大返回 Token 数
超出限制时自动截断
截断时保留关键信息（头部 + 尾部）
4.2 返回结果摘要
工具返回原始结果 + 摘要
Agent 上下文中只注入摘要
需要细节时再获取原始数据
4.3 返回结果索引
大结果存入临时索引
返回索引引用 + 元数据摘要
Agent 通过索引按需检索
4.4 渐进式返回
先返回概要信息
Agent 判断需要哪些细节
按需获取详细数据
4.5 结构化返回
将非结构化大文本转为结构化数据
只返回结构化摘要
减少 Token 消耗同时保留关键信息

五、Agent 间大数据传递
5.1 共享存储模式
多 Agent 共享外部存储（数据库 / 对象存储 / 文件系统）
Agent 间传递存储引用而非原始数据
5.2 消息压缩模式
Agent 间消息的自动压缩
摘要传递 + 按需获取原始数据
5.3 数据管道模式
上游 Agent 将处理结果写入管道
下游 Agent 从管道读取
避免通过上下文传递大数据

六、长时任务概述
6.1 什么是长时任务
执行时间超过典型 HTTP 请求超时（>30 秒）的 Agent 任务
可能持续数分钟、数小时甚至数天
需要特殊的架构支持
6.2 长时任务的典型场景
大规模数据处理与分析
多步骤研究报告生成
代码库级别的重构
批量文档处理
复杂的多 Agent 协作任务
持续监控与响应任务
6.3 长时任务的核心挑战
连接超时（HTTP / WebSocket 超时）
状态丢失（进程崩溃、服务重启）
上下文累积（长时间执行导致上下文膨胀）
成本失控（Token 消耗随时间增长）
用户等待体验（长时间无反馈）
错误恢复（中途失败后的恢复成本）

七、长时任务架构模式
7.1 异步任务模式
提交任务 → 立即返回任务 ID → 轮询/推送结果
任务队列（Redis Queue / Celery / RabbitMQ）
任务状态机（submitted → working → completed / failed）
7.2 持久化执行（Durable Execution）
每个步骤完成后持久化状态
失败后从最近检查点恢复
不需要从头重新执行
框架支持
(1) LangGraph Checkpointing
(2) Temporal
(3) Inngest
(4) AWS Step Functions
7.3 MCP Tasks（实验性）
MCP 2025-11-25 规范引入的 Tasks 原语
call-now, fetch-later 模式
任务句柄（durable handle）
轮询或订阅任务状态
标准化长时间操作的跨生态互操作
7.4 A2A 异步任务
A2A 协议的异步任务支持
Push Notification 状态推送
任务的 submitted → working → completed 生命周期

八、检查点与状态持久化
8.1 检查点策略
每步检查点（每个工具调用后保存）
关键步骤检查点（仅在重要节点保存）
定时检查点（每 N 秒保存一次）
8.2 检查点内容
Agent 状态（当前计划、已完成步骤）
对话历史（压缩后）
中间结果
工具调用记录
环境状态快照
8.3 检查点存储
内存存储（开发环境）
SQLite（轻量级）
PostgreSQL（生产推荐）
Redis（高性能）
对象存储（大状态）
8.4 从检查点恢复
故障检测与自动恢复
恢复点选择（最近 vs 最稳定）
恢复后的状态验证
部分结果的保留与续接

九、进度报告与用户体验
9.1 进度报告机制
实时进度百分比
当前步骤描述
预计剩余时间
已完成的中间结果展示
9.2 流式反馈
SSE / WebSocket 实时推送
AG-UI 事件流（STEP_STARTED / STEP_FINISHED）
逐步展示中间结果
9.3 用户交互
长时任务中的用户确认节点
用户可随时取消任务
用户可调整任务参数
暂停与恢复
9.4 通知机制
任务完成通知（邮件 / 消息 / Webhook）
异常告警通知
里程碑通知

十、长时任务的资源管理
10.1 Token 预算控制
总 Token 预算设置
每步 Token 消耗监控
接近预算时的压缩策略
超出预算时的降级策略
10.2 成本控制
总成本预算
成本实时监控
成本超限告警与自动停止
10.3 时间控制
总任务超时设置
单步超时设置
超时后的优雅终止
10.4 并发控制
最大并行任务数
资源竞争管理
优先级队列

十一、长时任务的容错
11.1 故障恢复
自动从检查点恢复
重试失败的步骤（而非整个任务）
替代策略切换
11.2 部分结果处理
任务中途失败时返回已完成的部分结果
标注哪些部分未完成
用户可选择接受部分结果或重试
11.3 幂等性保证
每个步骤的幂等性设计
重试不会产生副作用
状态更新的原子性

十二、最佳实践与注意事项
大文本传递优先使用引用模式，避免直接注入上下文
工具返回结果应有大小限制，超出时自动摘要
长时任务必须有检查点机制
用户体验：进度反馈比最终结果更重要
Token 和成本预算是长时任务的生命线
常见错误
1. 大文本直接塞入上下文导致 Token 超限
2. 工具返回未截断导致上下文膨胀
3. 长时任务无检查点导致失败后从头开始
4. 缺乏进度反馈导致用户以为系统卡死
5. 未设置总超时导致任务无限运行
6. 忽视成本控制导致账单爆炸
7. Agent 间传递大数据通过上下文而非共享存储
