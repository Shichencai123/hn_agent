一、Agent 系统工程概述
1.1 Agent 系统工程的定义
Agent 从原型到生产的工程化实践
涵盖流式输出、并发控制、异步调度等基础设施
区别于 Agent 逻辑设计的底层系统能力
1.2 Agent 系统的技术栈分层
应用层：Agent 逻辑、Prompt、工具编排
服务层：API 网关、流式传输、任务调度
基础设施层：消息队列、缓存、数据库、监控
1.3 生产环境的核心挑战
延迟敏感（用户等待体验）
高并发（多用户同时使用）
长时任务（分钟到小时级任务）
成本控制（LLM API 调用费用）
可靠性（99.9% 可用性要求）

二、流式输出（Streaming）
2.1 为什么需要流式输出
非流式的用户体验问题（长时间空白等待）
流式的感知延迟优化（首 Token 200-400ms 内可见）
总时间相近，但用户体验截然不同
2.2 流式传输协议
1. Server-Sent Events（SSE）
基于 HTTP 的单向流
Content-Type: text/event-stream
data: 行格式
自动重连机制
代理友好（穿透 Nginx / CDN）
适用：大多数 LLM 流式场景（推荐默认选择）
2. WebSocket
全双工双向通信
持久连接
适用：需要双向实时通信（协同编辑、多方对话）
3. HTTP/2 Server Push
多路复用
适用：受控环境、服务端组件框架
4. 协议选择指南
单向流式（LLM 输出）→ SSE
双向实时（协同交互）→ WebSocket
服务端渲染 → HTTP/2
2.3 LLM API 流式调用
OpenAI stream=True
Anthropic stream=True
流式响应的解析（逐 chunk 处理）
流式 JSON 的增量解析
2.4 Agent 多层流式架构
LLM API → Agent 后端 → API 网关 → 前端 UI
每一层的流式传递与缓冲
端到端流式的延迟优化
2.5 流式输出的内容类型
Token 流（逐 Token 文本生成）
工具调用流（工具名称 → 参数 → 结果）
状态更新流（Agent 执行进度）
多 Agent 流（多个 Agent 的交错输出）
2.6 流式缓冲与批处理
Token 级缓冲（每个 Token 立即发送）
时间窗口缓冲（每 50-100ms 批量发送）
语义缓冲（按句子或段落发送）
缓冲策略对用户体验的影响
2.7 流式中的工具调用处理
工具调用参数的流式累积
工具执行期间的等待指示
工具结果的流式注入
AG-UI 事件类型的映射
2.8 流式错误处理
连接中断的检测与重连
中途失败的部分结果保存
流式超时处理
客户端断开的服务端清理
2.9 流式输出的关键指标
TTFT（Time to First Token）：首 Token 延迟，目标 < 500ms
TPS（Tokens Per Second）：Token 生成速率
TTLR（Time to Last Response）：完整响应时间
流式中断率

三、高并发下的并发控制
3.1 Agent 系统的并发模型
请求级并发（多用户同时请求）
任务级并发（单用户多任务并行）
Agent 级并发（多 Agent 并行执行）
工具级并发（多工具并行调用）
3.2 LLM API 限流管理
速率限制类型
(1) RPM（Requests Per Minute）
(2) TPM（Tokens Per Minute）
(3) 并发请求数限制
限流应对策略
(1) 令牌桶算法（Token Bucket）
(2) 滑动窗口计数器
(3) 请求排队与优先级
(4) 多 API Key 轮换
(5) 多提供商负载均衡
429 响应处理
(1) Retry-After Header 遵循
(2) 指数退避 + 抖动
(3) 降级到备用模型
3.3 请求排队与优先级
请求队列设计
(1) FIFO 队列（公平排队）
(2) 优先级队列（VIP 用户优先）
(3) 多级队列（不同任务类型不同队列）
队列深度控制
(1) 最大队列长度
(2) 队列满时的拒绝策略
(3) 队列等待超时
3.4 并发池管理
LLM 调用并发池
(1) 最大并发数限制
(2) 信号量（Semaphore）控制
(3) 连接池复用
工具调用并发池
(1) 按工具类型分池
(2) 外部 API 的并发限制
(3) 数据库连接池
Agent 执行并发池
(1) 最大同时运行 Agent 数
(2) Agent 资源隔离
(3) 资源抢占与优先级
3.5 背压控制（Backpressure）
什么是背压（下游处理不过来时的上游控制）
背压信号传递
(1) 队列深度超阈值
(2) 响应延迟超阈值
(3) 错误率超阈值
背压应对策略
(1) 减缓请求接收速率
(2) 丢弃低优先级请求
(3) 降级处理（简化 Agent 流程）
(4) 自动扩容触发
3.6 多租户并发隔离
租户级资源配额
租户间的公平调度
防止单租户耗尽资源（noisy neighbor 问题）
租户级限流与计费

四、Agent 异步任务调度
4.1 为什么需要异步任务调度
同步请求-响应模式的局限（HTTP 超时、连接占用）
Agent 任务的长时间特性（秒到小时级）
资源的高效利用（避免空等待）
4.2 异步任务架构
任务提交 → 消息队列 → Worker 消费 → 结果存储 → 通知客户端
核心组件
(1) 任务生产者（API 服务）
(2) 消息代理（Message Broker）
(3) 任务消费者（Worker）
(4) 结果存储（Result Backend）
(5) 任务调度器（Scheduler）
4.3 消息队列选型
Redis（轻量级，适合中小规模）
RabbitMQ（功能丰富，企业级）
Kafka（高吞吐，适合大规模流处理）
选型对比（延迟 / 吞吐量 / 持久化 / 复杂度）
4.4 任务调度框架
Celery（Python 生态最成熟）
(1) Task 定义与注册
(2) Worker 管理
(3) 任务路由（按队列分发）
(4) 任务编排（chain / group / chord）
(5) 定时任务（Celery Beat）
(6) 重试与错误处理
RQ（Redis Queue，轻量替代）
Dramatiq（现代 Python 任务队列）
Temporal（持久化工作流引擎）
Inngest（事件驱动持久化执行）
4.5 Agent 任务的生命周期管理
任务状态机
(1) pending：等待执行
(2) started：开始执行
(3) running：执行中
(4) paused：暂停（等待人工输入）
(5) completed：完成
(6) failed：失败
(7) cancelled：取消
(8) retrying：重试中
任务状态持久化
任务状态查询 API
任务状态变更通知
4.6 任务优先级与调度策略
优先级队列（Critical > High > Normal > Low）
公平调度（防止高优先级饿死低优先级）
延迟任务（定时执行）
周期任务（定期执行）
依赖任务（前置任务完成后触发）
4.7 Worker 管理
Worker 数量的动态调整（自动扩缩容）
Worker 健康检查
Worker 优雅关闭（完成当前任务后退出）
Worker 资源隔离（CPU / 内存 / GPU）
4.8 任务结果管理
结果存储后端（Redis / PostgreSQL / S3）
结果过期与清理
大结果的存储策略（引用 vs 内联）
结果的流式返回

五、任务进度与实时通知
5.1 进度报告机制
任务内部进度更新
(1) 百分比进度
(2) 当前步骤描述
(3) 中间结果预览
进度存储（Redis Pub/Sub / 数据库轮询）
5.2 实时通知方式
SSE 推送（服务端主动推送）
WebSocket 推送（双向通信）
轮询（Polling，最简单但效率低）
Webhook 回调（任务完成时通知）
5.3 多 Agent 任务的进度聚合
各子 Agent 进度的汇总
整体进度的计算
并行子任务的进度展示

六、连接管理与资源优化
6.1 连接池管理
HTTP 连接池（LLM API 调用）
数据库连接池
Redis 连接池
连接池大小的调优
6.2 缓存策略
LLM 响应缓存（相同输入缓存输出）
(1) 精确匹配缓存
(2) 语义相似度缓存
工具结果缓存
Embedding 缓存
缓存失效策略（TTL / LRU / 手动失效）
6.3 资源监控与自动扩缩容
CPU / 内存 / GPU 使用率监控
队列深度监控
自动扩容触发条件
缩容策略（避免频繁扩缩）

七、API 网关与负载均衡
7.1 API 网关设计
请求路由
认证与授权
限流与熔断
请求/响应转换
流式代理（SSE / WebSocket 透传）
7.2 负载均衡策略
轮询（Round Robin）
加权轮询（Weighted Round Robin）
最少连接（Least Connections）
一致性哈希（会话亲和性）
7.3 LLM Gateway（AI 网关）
多提供商路由
自动故障转移
成本优化路由
Token 使用追踪
工具推荐：Portkey / LiteLLM / OpenRouter

八、可观测性与运维
8.1 监控指标体系
业务指标：任务完成率、用户满意度
性能指标：TTFT、TPS、P50/P95/P99 延迟
资源指标：CPU、内存、GPU、连接数
成本指标：Token 消耗、API 调用次数、总费用
8.2 日志体系
结构化日志（JSON 格式）
请求链路日志（Trace ID 贯穿）
Agent 推理日志
工具调用日志
8.3 告警体系
延迟告警（P99 超阈值）
错误率告警
队列积压告警
成本超预算告警
8.4 运维工具
Prometheus + Grafana（指标监控）
ELK / Loki（日志管理）
Jaeger / Tempo（链路追踪）
LangSmith / Langfuse（Agent 专用可观测性）

九、最佳实践与注意事项
流式输出是用户体验的基础，不是可选项
并发控制从 API 限流开始，逐层向上
异步任务调度是长时任务的必备架构
背压控制防止系统雪崩
缓存是成本优化的第一手段
监控先行，没有可观测性就没有可靠性
常见错误
1. 忽视流式输出导致用户体验差
2. 未做限流导致 LLM API 429 错误频发
3. 同步处理长时任务导致连接超时
4. 缺乏背压控制导致系统过载崩溃
5. Worker 数量固定导致资源浪费或不足
6. 缓存策略不当导致数据过时或内存溢出
7. 监控缺失导致问题发现滞后
