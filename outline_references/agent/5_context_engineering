一、上下文工程概述
1.1 什么是上下文工程（Context Engineering）
Andrej Karpathy 的定义：填充上下文窗口的艺术与科学
上下文工程 vs Prompt 工程的区别与演进
上下文窗口 = LLM 的 RAM（工作内存）
上下文工程的核心目标：在正确的时间提供正确的信息
1.2 为什么上下文工程比 Prompt 工程更重要
Prompt 工程的局限性（静态、单轮、手工）
Agent 场景下的上下文复杂性（多轮、多源、动态）
上下文质量对 Agent 性能的决定性影响
1.3 上下文工程的核心原则
每个 Token 都应有价值（信噪比最大化）
动态组装而非静态模板
上下文是稀缺资源，需要精心管理
信息的时效性、相关性、可靠性三重过滤
1.4 上下文的组成要素
系统指令（System Instructions）
用户输入（User Input）
对话历史（Conversation History）
检索内容（RAG / Knowledge Base）
工具定义与结果（Tool Schemas & Results）
记忆（Short-term & Long-term Memory）
环境状态（Environment State）
元数据与约束（Metadata & Constraints）

二、上下文窗口管理
2.1 上下文窗口的本质
Token 限制与有效利用率
窗口大小与模型性能的关系（Lost in the Middle 问题）
注意力分布：U 型曲线（开头和结尾权重更高）
2.2 上下文预算分配
各组成部分的 Token 预算规划
固定预算 vs 动态预算
优先级排序策略
预算监控与动态调整
2.3 上下文溢出处理
溢出检测机制
优先级截断策略
紧急压缩策略
降级方案

三、上下文工程四大操作
3.1 写入（Write）—— 持久化上下文
将信息写入外部存储以备后续使用
写入目标
(1) 短期记忆（会话状态）
(2) 长期记忆（向量库 / 知识图谱）
(3) Scratchpad（临时工作空间）
(4) 文件系统（持久化文档）
写入时机与策略
写入格式（自然语言 / 结构化 / 向量）
3.2 选择（Select）—— 检索相关上下文
从外部存储中选择与当前任务最相关的信息
选择来源
(1) RAG 检索（向量相似度）
(2) 记忆检索（短期 + 长期）
(3) 工具结果获取
(4) 知识图谱遍历
(5) 数据库查询
选择策略
(1) 语义相似度匹配
(2) 关键词过滤
(3) 时间衰减加权
(4) 重要性排序
(5) 多路召回 + 重排序
选择质量的关键指标
3.3 压缩（Compress）—— 减少 Token 消耗
在保留关键信息的前提下减少上下文体积
压缩技术
(1) 对话历史摘要
(2) 检索结果摘要
(3) 工具输出精简
(4) 冗余信息去除
(5) 关键信息提取
压缩策略
(1) 增量压缩（每 N 轮压缩一次）
(2) 阈值触发压缩（Token 数超过阈值时）
(3) 分层压缩（近期保留原文，远期压缩为摘要）
压缩质量评估（信息保留率）
3.4 隔离（Isolate）—— 防止上下文污染
将不同来源或用途的上下文隔离，避免相互干扰
隔离策略
(1) 子 Agent 上下文隔离
(2) 工具结果与对话历史隔离
(3) 系统指令与用户输入隔离
(4) 多用户上下文隔离
隔离的实现方式
(1) 独立上下文窗口
(2) 标签化分区
(3) 命名空间隔离

四、系统指令工程
4.1 System Prompt 设计
角色定义与行为规范
能力边界与约束条件
输出格式要求
错误处理指导
4.2 指令的层级结构
全局指令（始终生效）
任务级指令（特定任务生效）
步骤级指令（特定步骤生效）
4.3 指令的动态注入
根据任务类型动态选择指令集
根据执行阶段调整指令
条件指令（满足条件时激活）
4.4 指令文件化（Instructions-as-Code）
CLAUDE.md / .cursorrules / .kiro/steering 等模式
版本控制与协作
机器可读的指令格式

五、工具上下文工程
5.1 工具定义的上下文设计
工具描述的信噪比优化
参数描述的精确性
使用场景的明确标注
5.2 工具结果的上下文处理
结果格式化与精简
大结果的截断与摘要
结果中的内联指令（Tool Response as Prompt）
5.3 工具数量与上下文开销
工具定义占用的 Token 预算
工具数量对选择准确率的影响
工具分组与按需加载
5.4 上下文污染防护
工具输出中的噪声过滤
日志/调试信息的隔离
错误堆栈的精简

六、RAG 与上下文工程的交叉
6.1 RAG 作为上下文的动态供给
检索结果的上下文注入策略
检索质量对上下文质量的影响
6.2 检索结果的上下文优化
结果排序与截断
结果去重与合并
结果格式化（添加来源、置信度标注）
6.3 Agentic RAG 的上下文管理
Agent 驱动的动态检索
多轮检索的上下文累积
检索-推理-再检索的循环

七、多 Agent 上下文工程
7.1 Agent 间的上下文传递
完整传递 vs 摘要传递 vs 结构化传递
上下文大小控制
7.2 共享上下文管理
全局上下文 vs 局部上下文
上下文同步与一致性
7.3 Handoff 时的上下文工程
上下文快照与恢复
最小上下文原则
上下文格式转换

八、上下文工程的评估与优化
8.1 评估指标
上下文利用率（有效 Token / 总 Token）
信噪比（相关信息 / 总信息）
任务完成率与上下文质量的相关性
Token 成本
8.2 优化方法
A/B 测试不同上下文策略
上下文消融实验（逐步移除组件观察影响）
Token 预算优化
8.3 可观测性
上下文组成的可视化
各组件 Token 占比分析
上下文变化追踪

九、最佳实践与注意事项
上下文工程是系统工程，不是 Prompt 技巧
优先保证信噪比，而非信息量
动态上下文优于静态模板
定期审计上下文组成，清理低价值信息
上下文工程需要与 Agent 架构协同设计
常见错误
1. 上下文过载（塞入过多信息导致模型困惑）
2. 上下文饥饿（关键信息缺失导致幻觉）
3. 上下文污染（无关信息干扰推理）
4. 上下文过时（未及时更新导致错误决策）
5. 忽视 Token 成本（上下文膨胀导致成本失控）
