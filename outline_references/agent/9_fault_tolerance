一、Agent 容错降级概述
1.1 为什么 Agent 系统需要容错
LLM 的非确定性本质（同一输入可能产生不同输出）
外部依赖的不可靠性（API 超时、限流、宕机）
Agent 特有的失败模式（幻觉、死循环、上下文溢出）
生产环境的可靠性要求
1.2 Agent 失败的四大类型
语法错误（Syntactic）：输出格式不合法（JSON 解析失败等）
语义错误（Semantic）：输出合法但内容错误（幻觉、逻辑矛盾）
环境错误（Environmental）：外部系统故障（API 超时、网络中断）
行为错误（Behavioral）：Agent 行为异常（死循环、目标偏离、工具滥用）
1.3 容错降级的核心原则
快速失败，优雅降级（Fail Fast, Degrade Gracefully）
部分结果优于无结果
用户感知最小化
自动恢复优先，人工介入兜底
每一层都有备份方案
1.4 容错降级的分层模型
L0：预防层（防止错误发生）
L1：检测层（快速发现错误）
L2：恢复层（自动修复错误）
L3：降级层（降低服务质量但保持可用）
L4：兜底层（人工介入或安全终止）

二、LLM 调用层容错
2.1 LLM API 常见故障
API 超时（Timeout）
速率限制（Rate Limit / 429）
服务不可用（503 / 500）
Token 超限（Context Length Exceeded）
内容过滤拦截（Content Filter）
模型不可用（Model Deprecated / Unavailable）
2.2 重试策略（Retry）
指数退避（Exponential Backoff）
(1) 基础延迟 × 2^attempt
(2) 最大延迟上限（cap）
(3) 随机抖动（Jitter）防止雷群效应
可重试错误 vs 不可重试错误
(1) 可重试：超时、429、503、网络错误
(2) 不可重试：401 认证失败、400 参数错误、内容过滤
最大重试次数（通常 3-5 次）
Retry-After Header 遵循
2.3 模型回退链（Model Fallback Chain）
主模型 → 备用模型 → 降级模型
同提供商回退（GPT-4o → GPT-4o-mini）
跨提供商回退（OpenAI → Anthropic → 开源模型）
回退时的 Prompt 适配
回退时的能力降级评估
2.4 熔断器（Circuit Breaker）
三态模型：Closed → Open → Half-Open
(1) Closed：正常调用，监控失败率
(2) Open：失败率超阈值，直接返回降级结果
(3) Half-Open：定期探测，恢复则关闭
熔断阈值设置（失败率 / 时间窗口）
熔断时的降级响应
2.5 超时管理
单次 LLM 调用超时（通常 30-60 秒）
流式输出的首 Token 超时
总任务超时
超时后的部分结果处理
2.6 Token 超限处理
上下文压缩（摘要历史对话）
优先级截断（保留高优先级信息）
切换到更大上下文窗口的模型
分批处理（拆分为多次调用）
2.7 LLM Gateway / AI Gateway
统一入口管理多个 LLM 提供商
自动故障转移路由
负载均衡与成本优化
工具推荐：Portkey / LiteLLM / OpenRouter / Helicone

三、LLM 输出层容错
3.1 输出格式校验
JSON 解析失败处理
(1) 自动修复（提取 JSON 片段、修复常见格式错误）
(2) 重新生成（要求 LLM 重新输出）
(3) 使用 Structured Output / JSON Mode 强制格式
Pydantic 模型验证
Schema 校验与类型检查
3.2 输出内容校验
幻觉检测（事实一致性检查）
逻辑一致性验证
输出完整性检查（是否遗漏必要字段）
敏感信息过滤
3.3 Try-Rewrite-Retry 模式
LLM 输出错误 → 将错误信息反馈给 LLM → LLM 自我修正
最大修正轮次限制
修正失败后的降级策略
3.4 输出降级策略
无法生成结构化输出 → 降级为自由文本
无法生成完整回答 → 返回部分回答 + 说明
无法生成任何有效输出 → 返回预设的安全回答

四、工具调用层容错
4.1 工具调用常见故障
工具不存在（Agent 幻觉出不存在的工具）
参数错误（类型不匹配、缺少必填参数、格式错误）
工具执行失败（API 超时、权限不足、服务不可用）
工具返回异常结果（空结果、错误数据、超大响应）
4.2 工具调用前校验
工具名称校验（是否在可用工具列表中）
参数 Schema 校验（类型、格式、必填项）
参数自动修正（类型转换、默认值填充）
权限预检查
4.3 工具执行容错
单工具重试（指数退避）
替代工具切换
(1) 同功能不同实现（如不同搜索引擎）
(2) 降级工具（功能更简单但更可靠）
工具执行超时控制
工具执行沙箱隔离
4.4 工具结果处理
空结果处理（提示 Agent 换个方式尝试）
超大结果截断（限制注入上下文的大小）
错误结果识别（检测明显异常的返回值）
结果缓存（避免重复调用）
4.5 工具层降级策略
工具完全不可用 → Agent 基于自身知识回答
搜索工具不可用 → 使用缓存的历史结果
代码执行工具不可用 → LLM 直接推理计算
数据库工具不可用 → 使用预加载的静态数据

五、Agent 推理层容错
5.1 死循环检测与防护
重复行为检测（连续 N 次执行相同操作）
最大迭代次数限制
最大 Token 消耗限制
无进展检测（连续多步没有实质性推进）
强制终止与部分结果返回
5.2 目标偏离检测
定期检查当前行为与原始目标的对齐度
偏离阈值触发重规划
严重偏离触发人工介入
5.3 推理质量保障
自我一致性检查（多次推理取共识）
中间结果验证
关键决策点的置信度评估
低置信度时的保守策略
5.4 规划失败处理
任务分解失败 → 简化任务描述重试
子任务无法执行 → 跳过并标注
计划执行偏离 → 动态重规划
多次重规划失败 → 降级为简单直接回答

六、记忆与状态层容错
6.1 记忆检索失败
向量数据库不可用 → 降级为无记忆模式
检索结果为空 → 扩大检索范围或放宽阈值
检索结果不相关 → 过滤后使用 Agent 自身知识
6.2 状态持久化失败
检查点写入失败 → 内存缓存 + 异步重试
检查点读取失败 → 从最近可用检查点恢复
状态损坏 → 从头开始执行（最后手段）
6.3 上下文溢出处理
上下文接近上限 → 触发压缩策略
压缩后仍超限 → 优先级截断
截断后信息不足 → 降级为简化模式
6.4 记忆一致性故障
新旧记忆冲突 → 以最新信息为准
记忆与事实矛盾 → 标记冲突，优先使用实时信息
跨 Agent 记忆不一致 → 触发同步或以权威源为准

七、多 Agent 协同层容错
7.1 子 Agent 失败处理
子 Agent 执行失败 → 重试（同一 Agent）
重试失败 → 替代 Agent 接管
替代 Agent 也失败 → 主 Agent 自行处理（降级）
所有方案失败 → 返回部分结果 + 失败说明
7.2 通信故障处理
Agent 间消息丢失 → 消息确认与重发
Agent 间消息超时 → 超时后使用默认值继续
消息格式错误 → 自动修正或请求重发
7.3 协调失败处理
Supervisor 不可用 → 子 Agent 自主执行预定义流程
共识无法达成 → 超时后采用多数投票结果
任务分配冲突 → 仲裁机制或优先级规则
7.4 级联故障防护
故障隔离（子 Agent 故障不影响其他子 Agent）
熔断机制（某个子 Agent 持续失败时停止调用）
降级编排（减少参与的 Agent 数量，简化流程）

八、RAG 检索层容错
8.1 检索失败处理
向量数据库不可用 → 切换到备用数据库
检索超时 → 返回缓存结果
检索结果为空 → 扩大查询范围 / 查询改写
8.2 检索质量降级
检索结果相关性低 → 重排序 / 多路召回
检索结果过时 → 标注时间戳提醒 Agent
检索结果矛盾 → 多源交叉验证
8.3 Embedding 服务故障
Embedding API 不可用 → 切换到本地 Embedding 模型
Embedding 质量下降 → 回退到关键词检索
8.4 RAG 降级策略
完整 RAG 不可用 → 降级为纯 LLM 回答（标注无外部知识）
部分检索失败 → 使用已成功检索的部分结果
所有检索失败 → 明确告知用户信息可能不完整

九、MCP / 工具服务层容错
9.1 MCP Server 故障
Server 连接失败 → 重试连接
Server 响应超时 → 超时后跳过该 Server
Server 持续不可用 → 熔断 + 降级到替代方案
9.2 多 Server 容错
主 Server 不可用 → 切换到备用 Server
多个 Server 提供相同功能 → 负载均衡 + 故障转移
所有 Server 不可用 → 降级到 Agent 内置能力
9.3 工具版本兼容
Server 升级导致接口变更 → 版本协商
工具参数变更 → Schema 校验 + 自动适配
工具废弃 → 自动切换到替代工具

十、Human-in-the-Loop 容错
10.1 人工介入触发条件
Agent 连续失败超过阈值
涉及高风险操作（资金、数据删除、对外发送）
Agent 置信度低于阈值
用户主动请求人工介入
10.2 人工介入模式
同步审批（Agent 暂停等待人工决策）
异步审批（Agent 继续其他任务，等待审批结果）
升级处理（转交给人工专家完全接管）
10.3 人工介入超时处理
审批超时 → 执行默认安全策略
审批超时 → 自动取消操作
审批超时 → 降级执行（执行风险更低的替代方案）
10.4 人工反馈闭环
人工修正 → 记录修正经验
经验积累 → 优化 Agent 行为
减少未来的人工介入频率

十一、全局容错架构
11.1 多层防御体系
预防 → 检测 → 恢复 → 降级 → 兜底
每一层都有独立的容错机制
层层递进，逐步降级
11.2 全局超时与预算控制
总任务超时（端到端）
总 Token 预算
总成本预算
超出预算时的降级策略
11.3 幂等性设计
工具调用的幂等性保证
状态更新的幂等性
重试安全性
11.4 审计与回溯
完整的执行日志
错误链路追踪
故障复盘与根因分析
11.5 混沌工程
主动注入故障测试容错能力
模拟 LLM API 故障
模拟工具调用失败
模拟网络中断
验证降级策略的有效性

十二、最佳实践与注意事项
容错设计应从第一天开始，而非事后补救
每个外部依赖都需要备份方案
降级不等于失败，部分结果优于无结果
监控是容错的前提（看不到问题就无法处理）
定期测试容错机制（混沌工程）
常见错误
1. 只有重试没有降级（重试耗尽后系统崩溃）
2. 降级方案未经测试（降级路径本身有 Bug）
3. 忽视级联故障（一个组件故障拖垮整个系统）
4. 超时设置不合理（太短导致误判，太长导致用户等待）
5. 缺乏全局预算控制（Token/成本失控）
6. 人工介入流程缺失（Agent 无法自行恢复时无路可走）
