一、LangGraph 概述
1.1 什么是 LangGraph
低级别 Agent 编排框架与运行时
基于有向图的状态机模型
LangChain 团队开发，2025.10 发布 1.0 GA
1.2 LangGraph 的核心定位
LangChain 负责组件与集成
LangGraph 负责编排与控制流
LangGraph 是构建生产级 Agent 的推荐方式
1.3 为什么需要 LangGraph
线性 Chain 的局限性（无法处理循环、分支、错误恢复）
Agent 需要有状态的、可循环的工作流
生产环境需要检查点、人工介入、流式输出
1.4 LangGraph 的核心特性
图结构工作流（节点 + 边）
有状态执行（State Schema）
循环支持（Cycles）
检查点与持久化（Checkpointing）
Human-in-the-Loop（中断与恢复）
流式输出（Streaming）
子图嵌套（Subgraphs）
1.5 LangGraph vs 其他框架
LangGraph vs LangChain AgentExecutor
LangGraph vs CrewAI
LangGraph vs AutoGen
LangGraph vs OpenAI Agents SDK

二、核心概念
2.1 图（Graph）
StateGraph：有状态图
MessageGraph：消息图（简化版）
图的编译（compile）
图的可视化
2.2 状态（State）
State Schema 定义（TypedDict / Pydantic）
状态的读写
状态的 Reducer 函数（合并策略）
Annotation 语法
消息状态（MessagesState / add_messages）
2.3 节点（Nodes）
节点 = Python 函数
节点的输入输出（接收 State，返回 State 更新）
节点类型
(1) LLM 调用节点
(2) 工具执行节点
(3) 条件判断节点
(4) 人工输入节点
(5) 子图节点
2.4 边（Edges）
普通边（Normal Edge）：固定路由
条件边（Conditional Edge）：动态路由
入口点（START）与终点（END）
2.5 编译与执行
graph.compile()
graph.invoke() / graph.stream()
执行配置（config）
线程管理（thread_id）

三、状态管理
3.1 State Schema 设计
TypedDict 定义
Pydantic BaseModel 定义
字段类型与默认值
3.2 状态更新机制
节点返回部分状态更新
Reducer 函数（状态合并策略）
默认 Reducer（覆盖）
自定义 Reducer（追加、合并等）
add_messages Reducer（消息列表管理）
3.3 消息状态管理
MessagesState 预置状态
消息的追加与修改
消息历史的管理
3.4 状态的作用域
全局状态（所有节点共享）
节点私有状态
子图状态隔离
3.5 状态的序列化与持久化
JSON 序列化
自定义序列化器
状态大小控制

四、检查点与持久化（Checkpointing）
4.1 检查点的核心概念
每个节点执行后自动保存状态快照
支持状态恢复与时间旅行
线程（Thread）与检查点的关系
4.2 Checkpointer 实现
MemorySaver（内存，开发用）
SqliteSaver（SQLite）
PostgresSaver（PostgreSQL，生产推荐）
自定义 Checkpointer
4.3 线程管理
thread_id 的作用
多线程并发
线程的生命周期
4.4 时间旅行（Time Travel）
回溯到历史检查点
从历史状态重新执行
状态分叉（Fork）
4.5 短期记忆与长期记忆
检查点作为短期记忆（会话内）
Store 作为长期记忆（跨会话）
记忆的检索与注入

五、Human-in-the-Loop
5.1 中断机制（Interrupts）
interrupt_before：节点执行前中断
interrupt_after：节点执行后中断
中断时的状态保存
5.2 人工审批流程
Agent 提出行动方案 → 暂停 → 人工审批 → 继续/修改
危险操作的审批
结果质量的审核
5.3 人工输入
在执行过程中请求用户输入
动态调整 Agent 行为
5.4 状态编辑
在中断时修改 Agent 状态
注入额外信息
修正错误状态

六、流式输出（Streaming）
6.1 流式模式
stream_mode="values"（完整状态流）
stream_mode="updates"（增量更新流）
stream_mode="messages"（消息级流）
stream_mode="events"（事件级流）
6.2 LLM Token 流式
逐 Token 流式输出
流式与非流式的混合
6.3 多节点流式
跨节点的流式传递
流式事件的过滤与路由

七、预构建 Agent
7.1 create_react_agent
ReAct 模式的快速构建
参数配置（model / tools / prompt / checkpointer）
自定义与扩展
7.2 langgraph-supervisor
Supervisor 多 Agent 模式
create_supervisor() 函数
Supervisor 的路由逻辑
Worker Agent 的注册
7.3 langgraph-swarm
Swarm 多 Agent 模式
Agent 间的 Handoff
动态 Agent 切换
会话记忆保持
7.4 Functional API
@entrypoint 装饰器
@task 装饰器
函数式 Agent 定义
与 Graph API 的对比

八、多 Agent 编排
8.1 Supervisor 模式
中心 Supervisor 协调多个 Worker
动态路由与任务分配
结果聚合
8.2 Hierarchical 模式（层级式）
多层级 Supervisor 树
子图嵌套实现层级
中间层 Agent 的协调
8.3 Swarm 模式
Agent 间的对等 Handoff
无中心协调者
动态 Agent 激活
8.4 自定义多 Agent 模式
图结构的灵活组合
条件路由实现复杂流程
并行执行与结果合并
8.5 子图（Subgraphs）
子图的定义与嵌套
子图的状态隔离
子图间的通信
子图的复用

九、工具集成
9.1 工具节点（ToolNode）
预构建的工具执行节点
工具调用的自动路由
工具结果的状态更新
9.2 工具的错误处理
工具调用失败的处理
重试策略
降级方案
9.3 MCP 集成
MCP Server 作为 LangGraph 工具
动态工具发现
MCP 工具的状态管理

十、生产部署
10.1 LangGraph Platform
LangGraph Server（Agent 部署服务）
LangGraph Studio（可视化调试工具）
LangGraph CLI
10.2 部署选项
LangGraph Cloud（托管服务）
BYOC（Bring Your Own Cloud，AWS VPC）
Self-Hosted Enterprise（自托管）
10.3 LangServe 部署
FastAPI 集成
REST API 端点
Docker 容器化
10.4 生产环境考量
性能优化（延迟、吞吐量）
扩展性（水平扩展）
监控与告警
成本控制

十一、可观测性与评估
11.1 LangSmith 集成
执行追踪（Tracing）
Prompt 管理
数据集与评估
11.2 调试技巧
图的可视化
状态检查
检查点回溯
节点级调试
11.3 评估方法
Agent 评估指标
基准测试
回归测试
A/B 测试

十二、最佳实践与注意事项
从 create_react_agent 开始，按需自定义
状态设计是 LangGraph 应用的核心
善用检查点实现容错和恢复
Human-in-the-Loop 是生产环境的必备
子图是管理复杂度的关键工具
常见错误
1. 状态设计不当导致信息丢失或膨胀
2. 忽视 Reducer 导致状态覆盖
3. 缺少终止条件导致无限循环
4. 检查点存储选择不当导致性能问题
5. 子图状态隔离不当导致数据泄露
6. 忽视流式输出导致用户体验差
