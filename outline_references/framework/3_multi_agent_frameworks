一、多 Agent 框架全景概述
1.1 多 Agent 框架的兴起背景
从单 Agent 到多 Agent 的演进
2023-2026 多 Agent 框架爆发期
框架选择的核心考量维度
1.2 主流框架速览
LangChain / LangGraph（图状态机编排）
AutoGen / AG2（对话驱动协作）
MetaGPT（SOP 驱动的软件开发）
AutoGPT（自主任务执行）
LlamaIndex（数据驱动的 Agent）
CrewAI（角色协作）
OpenAI Agents SDK（轻量 Handoff）
Google ADK（Google 生态）
1.3 框架分类维度
编排范式：图状态机 / 对话驱动 / SOP 流水线 / 自主循环
协作模式：层级式 / 对等式 / 混合式
抽象层级：低级（精细控制）/ 高级（快速构建）
生态定位：通用框架 / 领域框架 / 平台绑定

二、LangChain / LangGraph
2.1 架构设计
LangChain：模块化组件库（Models / Prompts / Tools / Memory / Chains）
LangGraph：基于有向图的状态机编排
两者的关系：LangChain 提供组件，LangGraph 提供编排
2.2 核心特性
LCEL 声明式链组合
StateGraph 有状态图
检查点与持久化
Human-in-the-Loop（中断与恢复）
流式输出
子图嵌套
2.3 多 Agent 模式
Supervisor 模式（langgraph-supervisor）
Swarm 模式（langgraph-swarm）
Hierarchical 模式（子图嵌套）
自定义图编排
2.4 通信机制
通过共享 State 通信
条件边实现动态路由
消息状态（MessagesState + add_messages Reducer）
2.5 优势
极高的灵活性和可控性
最成熟的状态管理和检查点机制
丰富的集成生态
LangSmith 可观测性
生产就绪度最高
2.6 劣势
学习曲线较陡（尤其 LangGraph）
简单场景过度工程化
概念较多（State / Node / Edge / Reducer / Checkpointer）
2.7 适用场景
复杂工作流、需要精细控制流程
需要检查点和恢复的长时任务
生产环境部署

三、AutoGen / AG2
3.1 架构设计
以对话为核心的多 Agent 协作
ConversableAgent 基类
对话模式驱动的编排
从 AutoGen 到 AG2 的演进
3.2 核心特性
灵活的对话模式
人类参与对话（Human-in-the-Loop）
代码执行能力
嵌套对话（Nested Chat）
3.3 多 Agent 模式
两人对话（Two-Agent Chat）
群聊（Group Chat）
(1) AutoPattern（LLM 自动选择发言者）
(2) RoundRobin（轮询）
(3) ManualPattern（手动指定）
(4) SelectorGroupChat
嵌套对话（Nested Chat）
Swarm 模式
3.4 通信机制
基于消息传递的对话
GroupChatManager 协调群聊
发言者选择策略（LLM 选择 / 轮询 / 自定义函数）
对话终止条件
3.5 优势
对话模式直觉化，易于理解
灵活的对话拓扑
强大的代码执行支持
丰富的对话模式
社区活跃（40K+ Stars）
3.6 劣势
状态管理较弱（相比 LangGraph）
生产就绪度中等
对话模式在复杂流程中可能不够精确
文档质量参差不齐
3.7 适用场景
需要 Agent 间讨论和协商的场景
代码生成与执行
研究实验和原型开发

四、MetaGPT
4.1 架构设计
模拟软件公司的组织结构
SOP（标准操作流程）驱动
角色专业化：产品经理 / 架构师 / 工程师 / QA
流水线式协作
4.2 核心特性
角色定义与 SOP 编码
结构化输出（每个角色输出标准化文档）
共享消息池（Message Pool）
中间产物验证
4.3 多 Agent 模式
流水线模式（Assembly Line）
需求 → 设计 → 编码 → 测试 → 文档
角色间的结构化交接
4.4 通信机制
共享消息池（Publish-Subscribe 模式）
Agent 订阅感兴趣的消息类型
结构化消息格式（文档、代码、测试用例）
非直接对话，而是通过产物传递
4.5 优势
SOP 保证流程规范性
结构化输出减少错误传播
模拟真实软件开发流程
中间产物可审查
4.6 劣势
流程较为固定，灵活性有限
主要面向软件开发场景
通用性不如 LangGraph / AutoGen
社区规模相对较小
4.7 适用场景
软件开发自动化
需要规范化流程的项目
文档驱动的开发

五、AutoGPT
5.1 架构设计
自主任务执行循环
目标 → 任务分解 → 执行 → 反思 → 迭代
Agent Protocol 标准
可视化工作流构建器
5.2 核心特性
自主目标分解
Web 浏览与信息收集
代码执行
文件读写
自我反思与改进
持续运行（Continuous Agent）
5.3 多 Agent 模式
单 Agent 自主循环为主
多 Agent 编排（较新版本支持）
触发器驱动的持续 Agent
5.4 通信机制
主要是 Agent 与工具的交互
Agent 内部的思考-行动-观察循环
较少的 Agent 间通信
5.5 优势
完全自主执行，最少人工干预
直觉化的目标驱动
Web 浏览能力强
Agent Protocol 标准化
5.6 劣势
自主性过高导致不可控
成本高（大量 LLM 调用）
容易陷入循环
生产就绪度较低
多 Agent 协作能力较弱
5.7 适用场景
探索性研究任务
自动化信息收集
个人助手原型

六、LlamaIndex
6.1 架构设计
以数据索引和检索为核心
Agent 能力围绕 RAG 构建
Workflows 事件驱动编排
LlamaParse 文档解析
LlamaCloud 企业级 RAG
6.2 核心特性
丰富的数据连接器（LlamaHub）
多种索引类型（Vector / Tree / Keyword / Knowledge Graph）
Query Engine 作为 Agent 工具
Workflows 事件驱动工作流
6.3 多 Agent 模式
Agent 模式（ReAct / Function Calling）
Workflows 编排（Chaining / Routing / Parallelization / Orchestrator-Workers）
多索引 Agent（不同索引作为不同工具）
6.4 通信机制
事件驱动（Event-based）
Step 之间通过 Event 传递数据
Workflow Context 共享状态
6.5 优势
RAG 场景最强（数据连接、解析、索引、检索全链路）
LlamaParse 文档解析能力突出
Workflows 事件驱动模型灵活
LlamaCloud 企业级支持
6.6 劣势
Agent 编排能力不如 LangGraph
多 Agent 协作支持较弱
非 RAG 场景的通用性不足
6.7 适用场景
知识密集型应用
RAG 系统构建
文档分析与问答

七、CrewAI
7.1 架构设计
模拟真实团队协作
三大核心概念：Agent（角色）/ Task（任务）/ Crew（团队）
Flows 事件驱动工作流
7.2 核心特性
角色定义（Role / Goal / Backstory）
任务委派（Delegation）
顺序执行（Sequential）/ 层级执行（Hierarchical）
原生 MCP 支持
7.3 多 Agent 模式
Sequential Process（顺序流水线）
Hierarchical Process（Manager Agent 协调）
Flows（事件驱动复杂工作流）
7.4 通信机制
任务输出作为下一个 Agent 的输入
Manager Agent 协调通信（Hierarchical 模式）
共享记忆
委派机制（Agent 可将子任务委派给其他 Agent）
7.5 优势与劣势
优势：最直觉化、上手最快、社区最大（44K+ Stars）
劣势：精细控制不如 LangGraph、状态管理较弱

八、OpenAI Agents SDK
8.1 架构设计
极简设计哲学
Handoff 为核心机制
内置 Guardrails
8.2 多 Agent 模式
Handoff 路由（Agent 间任务移交）
Triage Agent 模式（分诊路由）
8.3 优势与劣势
优势：极简 API、100 行代码构建多 Agent、内置安全护栏
劣势：仅支持 OpenAI 模型、状态管理基础

---

九、多 Agent 通信机制深度对比
9.1 通信范式分类
1. 共享状态通信（LangGraph）
所有 Agent 读写同一个 State 对象
通过 Reducer 函数管理状态合并
优势：强一致性、易于追踪
劣势：状态设计复杂、耦合度高
2. 消息传递通信（AutoGen / AG2）
Agent 之间通过消息对话
GroupChatManager 路由消息
优势：松耦合、灵活
劣势：消息顺序管理复杂
3. 产物传递通信（MetaGPT）
Agent 输出结构化产物（文档、代码）
下游 Agent 消费上游产物
优势：产物可审查、流程规范
劣势：灵活性低
4. 事件驱动通信（LlamaIndex Workflows）
Agent 之间通过 Event 传递
异步、解耦
优势：高度解耦、可扩展
劣势：调试复杂
5. Handoff 通信（OpenAI SDK / Swarm）
控制权完整移交
上下文随 Handoff 传递
优势：简单直接
劣势：不适合复杂协作
9.2 通信模式对比表
各框架的通信模式、同步/异步、状态共享方式、消息格式

十、多 Agent 角色协作模式
10.1 常见角色组合
Router（路由器）：分析意图，分发任务
Coder（编码者）：编写代码
Critic（评审者）：审查质量
Planner（规划者）：制定计划
Executor（执行者）：执行具体操作
Researcher（研究者）：搜索和收集信息
Writer（写作者）：生成文本内容
10.2 角色协作流程示例
代码生成场景
Router → Planner → Coder → Critic → Coder（修改）→ Critic（通过）
研究报告场景
Router → Researcher → Writer → Critic → Writer（修改）
10.3 各框架的角色实现方式
LangGraph：Node 函数 + 条件边路由
AutoGen：ConversableAgent + GroupChat
MetaGPT：Role 类 + SOP
CrewAI：Agent(role, goal, backstory) + Task

十一、状态管理对比
11.1 状态管理维度
状态定义方式
状态持久化能力
状态恢复能力
状态共享范围
11.2 各框架状态管理对比
LangGraph：TypedDict State + Checkpointer（最强）
AutoGen：对话历史 + GroupChat 状态（中等）
MetaGPT：Message Pool + 产物存储（中等）
LlamaIndex：Workflow Context（基础）
CrewAI：共享记忆（基础）
AutoGPT：Agent 内部状态（基础）
OpenAI SDK：Handoff 上下文传递（基础）
11.3 检查点与恢复能力
LangGraph：原生检查点，支持时间旅行
AutoGen：有限的状态保存
其他框架：大多需要自行实现

十二、冲突解决机制
12.1 冲突类型
意见冲突：多个 Agent 对同一问题有不同答案
资源冲突：多个 Agent 同时需要同一资源
状态冲突：并发状态更新导致不一致
优先级冲突：多个任务竞争执行优先级
12.2 各框架的冲突解决方式
LangGraph：Reducer 函数合并状态、条件边仲裁
AutoGen：GroupChatManager 选择发言者、对话协商
MetaGPT：SOP 流程避免冲突、产物验证
CrewAI：Manager Agent 仲裁、任务优先级
12.3 通用冲突解决策略
投票机制（多数决）
优先级规则（预定义优先级）
仲裁者模式（专门的仲裁 Agent）
乐观锁（检测冲突后重试）
最后写入胜出（Last Write Wins）

十三、框架综合对比与选型
13.1 综合对比表
编排灵活性 / 多 Agent 支持 / 状态管理 / 生产就绪度 / 学习曲线 / 社区活跃度 / 模型支持
13.2 选型决策树
需要精细控制 → LangGraph
需要 Agent 讨论 → AutoGen / AG2
软件开发自动化 → MetaGPT
自主任务执行 → AutoGPT
RAG 密集型 → LlamaIndex
快速原型 + 角色协作 → CrewAI
OpenAI 生态 + 极简 → OpenAI Agents SDK
13.3 混合使用模式
LangGraph 编排 + LlamaIndex RAG
CrewAI 角色定义 + LangGraph 状态管理
多框架通过 A2A 协议互操作

十四、最佳实践与注意事项
从最简单的框架开始，按需升级
框架选择应匹配团队技术栈
多 Agent 不一定优于单 Agent（先验证单 Agent 上限）
通信开销是多 Agent 系统的主要瓶颈
状态管理是生产环境的核心需求
常见错误
1. 选择过于复杂的框架导致开发效率低
2. 忽视框架的生产就绪度导致上线困难
3. Agent 角色设计不当导致协作低效
4. 通信机制选择不当导致信息丢失或膨胀
5. 缺乏冲突解决机制导致 Agent 死锁
6. 过度依赖单一框架导致锁定风险
